import world.Posn;

/**
 * Represents a smaller particle
 */
public class Mini extends Particle {

    Mini(Posn pos, int velx, int vely, boolean alliegence) {
        super(pos, "mini.png", velx, vely, alliegence);
    }

}
